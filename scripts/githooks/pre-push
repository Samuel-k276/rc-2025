#!/bin/sh
#
# Pre-push hook to format code before pushing
#

make format

# Check if there are any changes after formatting
if ! git diff --quiet; then
    echo "Warning: Code was reformatted. Please commit the changes before pushing."
    exit 1
fi

exit 0

